# Bar segment intake (fill when routes are wired)
PORT=8787
# Optional; Render does not need this (defaults to 0.0.0.0 in code)
# HOST=127.0.0.1
SEGMENT=bar

# Supabase (when submit-quote is implemented)
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
